const fs = require('fs');
let input = fs.readFileSync('stdin.txt').toString().split('\n');

const count = input[0];
const nums = [];

for (let i = 1; i <= count; i++) {
    nums.push(+input[i]);
}

const solution = (arr) => {
    const result = [];
    const nums = [1, 2, 3];

    const getComb = (numArr, target) => {};

    for (let i = 0; i < arr.length; i++) {
        const cur = arr[i];
    }
};

solution(arr);
