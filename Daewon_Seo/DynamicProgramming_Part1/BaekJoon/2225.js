const solution = (input) => {
    const [N, K] = input
        .toString()
        .trim()
        .split(' ')
        .map((el) => +el);

    const nArr = Array.from({ length: N + 1 }, (_, i) => i);
};

solution(`20 2`);
